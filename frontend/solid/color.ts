export const rgba = (r: number, g: number, b: number, a = 255): [number, number, number, number] => [r, g, b, a];

export const rgb = (r: number, g: number, b: number): [number, number, number, number] => [r, g, b, 255];
